#include "main.h"

/**
 * _abs - computes the abs value of an int
 * @n: the int to be checked
 *
 * Return: the absolute value of the int
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
