#include <stdio.h>
#include "main.h"
/**
 * main - returns the name of the argument
 * argc - argument count
 * argv - pointer to string argc
 * return: returns 0
 */
int main(int argc, char *argv[])
{
	printf("%s", argv[0]);
	return (0);
}
