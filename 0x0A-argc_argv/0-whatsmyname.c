#include <stdio.h>
#include "main.h"

/**
 * main -  function of the argument
 * @argc: argument count
 * @argv: pointer to string argc
 * Return: returns 0
 */

int main(int argc, char *argv[])
{
	printf("%s", argv[0]);
	return (0);
}
