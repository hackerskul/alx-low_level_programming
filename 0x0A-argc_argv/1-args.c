#include <stdio.h>

/**
 * main - main func
 * @argc: keeps the count pf the paremeters
 * @argv: pointer to the prog parameters
 * Return: none
 */

int main(int argc, char *argv[])
{
	printf("%d", argc);
}
